# 测试任务：TEST-OPEN-02
# 任务名称：Webhook系统测试
# 优先级：P1
# 预估工期：0.5天
# 测试类型：功能测试、集成测试
# 对应开发任务：TASK-OPEN-02

================================================================================
                          一、测试目标
================================================================================

验证Webhook系统功能，包括事件订阅、推送机制、重试策略、签名验证等。

================================================================================
                          二、测试环境
================================================================================

- **Webhook接收端**：测试服务器/webhook.site
- **测试事件类型**：记账、审批、客户变更等

================================================================================
                          三、测试用例
================================================================================

## 3.1 Webhook配置测试

### TC-OPEN-02-001 创建Webhook
**测试步骤**：
1. 进入开发者平台
2. 点击"配置Webhook"
3. 填写接收URL
4. 选择订阅事件
5. 设置密钥
6. 保存
**预期结果**：
- Webhook创建成功
- 状态：启用
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-002 配置验证
**测试步骤**：
1. 保存配置时进行验证请求
**预期结果**：
- 发送验证请求到URL
- 接收端返回200
- 验证成功
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-003 修改Webhook
**测试步骤**：
1. 修改URL或订阅事件
2. 保存
**预期结果**：
- 修改成功
- 新配置生效
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-004 启用/禁用Webhook
**测试步骤**：
1. 禁用Webhook
2. 触发订阅事件
**预期结果**：
- 禁用期间不推送事件
- 启用后恢复正常
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-005 删除Webhook
**测试步骤**：
1. 删除Webhook配置
**预期结果**：
- 删除成功
- 不再推送事件
**验收**：☐ 通过 ☐ 失败

## 3.2 事件推送测试

### TC-OPEN-02-006 记账事件推送
**测试步骤**：
1. 订阅"凭证创建"事件
2. 创建记账凭证
**预期结果**：
- 收到Webhook推送
- payload包含凭证信息
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-007 审批事件推送
**测试步骤**：
1. 订阅"审批状态变更"事件
2. 完成一次审批
**预期结果**：
- 收到审批完成推送
- 包含审批结果
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-008 客户变更推送
**测试步骤**：
1. 订阅"客户信息变更"事件
2. 修改客户信息
**预期结果**：
- 收到变更推送
- 包含变更前后对比
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-009 银行流水同步推送
**测试步骤**：
1. 订阅"银行流水同步"事件
2. 同步银行流水
**预期结果**：
- 收到同步完成推送
- 包含同步记录数
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-010 多事件订阅
**测试步骤**：
1. 配置同时订阅多个事件
2. 触发不同类型事件
**预期结果**：
- 各事件分别推送
- 事件类型标识正确
**验收**：☐ 通过 ☐ 失败

## 3.3 Webhook Payload测试

### TC-OPEN-02-011 Payload格式验证
**测试步骤**：
1. 接收Webhook请求
2. 检查payload结构
**预期结果**：
```json
{
  "event_type": "voucher.created",
  "event_id": "evt_xxx",
  "timestamp": 1704067200,
  "data": { ... }
}
```
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-012 事件ID唯一性
**测试步骤**：
1. 多次接收同一事件推送
**预期结果**：
- 事件ID唯一
- 可据此去重
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-013 时间戳准确性
**测试步骤**：
1. 检查payload中的timestamp
**预期结果**：
- 为Unix时间戳
- 与事件实际发生时间一致
**验收**：☐ 通过 ☐ 失败

## 3.4 签名验证测试

### TC-OPEN-02-014 签名生成
**测试步骤**：
1. 查看Webhook请求头
**预期结果**：
- 包含X-Signature头
- 使用HMAC-SHA256签名
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-015 签名验证
**测试步骤**：
1. 使用配置的密钥验证签名
**验证代码**：
```python
signature = hmac.new(
    secret.encode(),
    payload.encode(),
    hashlib.sha256
).hexdigest()
```
**预期结果**：
- 签名验证通过
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-016 签名防篡改
**测试步骤**：
1. 修改payload后验证签名
**预期结果**：
- 签名验证失败
- 应丢弃该请求
**验收**：☐ 通过 ☐ 失败

## 3.5 重试机制测试

### TC-OPEN-02-017 失败重试
**测试步骤**：
1. 接收端返回500错误
2. 观察重试行为
**预期结果**：
- 按指数退避重试
- 最多重试5次
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-018 重试间隔
**测试步骤**：
1. 记录每次重试时间
**预期结果**：
- 间隔：1s, 2s, 4s, 8s, 16s
**实测间隔**：
- 第1次：_______s
- 第2次：_______s
- 第3次：_______s
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-019 最终失败处理
**测试步骤**：
1. 接收端持续返回错误
2. 超过最大重试次数
**预期结果**：
- 停止重试
- 记录失败日志
- 可手动重发
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-020 成功响应
**测试步骤**：
1. 接收端返回200
**预期结果**：
- 不再重试
- 标记为推送成功
**验收**：☐ 通过 ☐ 失败

## 3.6 安全与性能测试

### TC-OPEN-02-021 HTTPS强制
**测试步骤**：
1. 配置HTTP地址
**预期结果**：
- 提示必须使用HTTPS
- 或自动升级（不推荐）
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-022 超时处理
**测试步骤**：
1. 接收端不响应（超时）
**预期结果**：
- 5秒后超时
- 进入重试流程
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-023 并发推送
**测试步骤**：
1. 短时间内触发大量事件
**预期结果**：
- 按顺序推送
- 不丢事件
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-02-024 推送日志
**测试步骤**：
1. 查看Webhook推送日志
**预期结果**：
- 显示每次推送详情
- 显示响应状态
- 支持重发
**验收**：☐ 通过 ☐ 失败

================================================================================
                          四、测试报告
================================================================================

**测试用例数**：24
**通过数**：_______
**失败数**：_______
**通过率**：_______%

**测试结论**：☐ 通过 ☐ 有条件通过 ☐ 不通过

**测试人员**：____________
**测试日期**：____________
