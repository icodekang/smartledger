# 测试任务：TEST-OPEN-01
# 任务名称：开放API平台测试
# 优先级：P1
# 预估工期：0.5天
# 测试类型：API测试、安全测试
# 对应开发任务：TASK-OPEN-01

================================================================================
                          一、测试目标
================================================================================

验证开放API平台功能，包括开发者注册、应用管理、API调用、权限控制等。

================================================================================
                          二、测试环境
================================================================================

- **API网关**：https://api.smartledger.ai
- **开发者平台**：https://developer.smartledger.ai
- **测试工具**：Postman/curl

================================================================================
                          三、测试用例
================================================================================

## 3.1 开发者注册与管理

### TC-OPEN-01-001 开发者注册
**测试步骤**：
1. 访问开发者平台
2. 点击"注册开发者账号"
3. 填写企业/个人资料
4. 提交审核
**预期结果**：
- 注册成功
- 等待审核（企业）或立即通过（个人）
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-002 实名认证
**测试步骤**：
1. 上传营业执照/身份证
2. 提交认证
**预期结果**：
- 认证信息提交成功
- 审核通过后状态更新
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-003 开发者信息修改
**测试步骤**：
1. 修改联系人、手机号
2. 保存
**预期结果**：
- 信息更新成功
**验收**：☐ 通过 ☐ 失败

## 3.2 应用管理测试

### TC-OPEN-01-004 创建应用
**测试步骤**：
1. 点击"创建应用"
2. 填写应用名称、描述
3. 选择应用类型
4. 提交
**预期结果**：
- 应用创建成功
- 分配AppKey和AppSecret
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-005 应用配置
**测试步骤**：
1. 配置回调地址
2. 配置IP白名单
3. 选择需要的API权限
**预期结果**：
- 配置保存成功
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-006 密钥重置
**测试步骤**：
1. 重置AppSecret
**预期结果**：
- 生成新的Secret
- 旧Secret立即失效
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-007 应用上线/下线
**测试步骤**：
1. 提交应用审核
2. 审核通过后上线
3. 手动下线应用
**预期结果**：
- 状态变更正确
- 下线后API调用被拒绝
**验收**：☐ 通过 ☐ 失败

## 3.3 API文档与调试

### TC-OPEN-01-008 API文档查看
**测试步骤**：
1. 查看API文档中心
**预期结果**：
- 文档完整清晰
- 包含请求参数、响应示例
- 支持在线调试
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-009 在线调试
**测试步骤**：
1. 选择API
2. 填写参数
3. 点击调试
**预期结果**：
- 显示请求和响应
- 响应数据格式化展示
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-010 SDK下载
**测试步骤**：
1. 下载Java/Python SDK
**预期结果**：
- SDK下载成功
- 包含使用示例
**验收**：☐ 通过 ☐ 失败

## 3.4 API认证测试

### TC-OPEN-01-011 AppKey认证
**测试步骤**：
```bash
curl -X GET "https://api.smartledger.ai/v1/customers" \
  -H "X-AppKey: your_app_key"
```
**预期结果**：
- 认证通过返回数据
- 无AppKey返回401
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-012 签名验证
**测试步骤**：
1. 按规则生成签名
2. 发送请求
**预期结果**：
- 签名正确返回200
- 签名错误返回403
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-013 Token刷新
**测试步骤**：
1. 使用refresh_token换取新access_token
**预期结果**：
- 返回新的access_token
- 旧token在有效期内仍可用
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-014 Token过期处理
**测试步骤**：
1. 使用过期token调用API
**预期结果**：
- 返回401，提示token过期
**验收**：☐ 通过 ☐ 失败

## 3.5 API功能测试

### TC-OPEN-01-015 客户列表API
**请求**：GET /v1/customers
**预期结果**：
- 返回客户列表
- 包含分页信息
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-016 客户详情API
**请求**：GET /v1/customers/{id}
**预期结果**：
- 返回客户详细信息
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-017 记账凭证API
**请求**：GET /v1/vouchers
**预期结果**：
- 返回凭证列表
- 支持筛选条件
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-018 创建凭证API
**请求**：POST /v1/vouchers
**预期结果**：
- 凭证创建成功
- 返回凭证ID
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-019 报表数据API
**请求**：GET /v1/reports/balance-sheet
**预期结果**：
- 返回资产负债表数据
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-020 Webhook订阅API
**请求**：POST /v1/webhooks
**预期结果**：
- 订阅创建成功
- 事件触发时收到通知
**验收**：☐ 通过 ☐ 失败

## 3.6 限流与配额测试

### TC-OPEN-01-021 速率限制
**测试步骤**：
1. 超过速率限制（如100次/分钟）频繁调用API
**预期结果**：
- 返回429 Too Many Requests
- 包含Retry-After头
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-022 日配额限制
**测试步骤**：
1. 超过日调用配额
**预期结果**：
- 返回429
- 提示配额已用完
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-023 配额查询
**测试步骤**：
1. 调用配额查询API
**预期结果**：
- 返回当前配额使用情况
**验收**：☐ 通过 ☐ 失败

## 3.7 安全测试

### TC-OPEN-01-024 IP白名单
**测试步骤**：
1. 配置IP白名单
2. 从非白名单IP调用
**预期结果**：
- 返回403 Forbidden
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-025 HTTPS强制
**测试步骤**：
1. 使用HTTP访问API
**预期结果**：
- 重定向到HTTPS
- 或拒绝访问
**验收**：☐ 通过 ☐ 失败

### TC-OPEN-01-026 敏感数据脱敏
**测试步骤**：
1. 调用返回客户数据的API
**预期结果**：
- 银行账号等敏感信息脱敏显示
**验收**：☐ 通过 ☐ 失败

================================================================================
                          四、性能测试
================================================================================

### PT-OPEN-01-001 API响应时间
**预期指标**：
- P99响应时间 ≤ 500ms
**实测结果**：_______ms
**验收**：☐ 通过 ☐ 失败

### PT-OPEN-01-002 并发处理能力
**预期指标**：
- 支持1000 QPS
**实测结果**：_______QPS
**验收**：☐ 通过 ☐ 失败

================================================================================
                          五、测试报告
================================================================================

**测试用例数**：26
**通过数**：_______
**失败数**：_______
**通过率**：_______%

**测试结论**：☐ 通过 ☐ 有条件通过 ☐ 不通过

**测试人员**：____________
**测试日期**：____________
