# SmartLedger 深度代码审查报告 - 第二版

**审查时间**: 2026-03-08  
**审查范围**: 前端 + 后端完整代码库

---

## 🔴 关键问题 (必须修复)

### 1. 前端 API 类型安全问题

**问题**: 前端 API 返回类型定义为 `any`

```typescript
// 当前代码 ❌
const res = await api.get('/invoices')
res.data.items  // 没有类型检查

// 应该 ✅
interface BillResponse {
  items: Bill[]
  total: number
}
const res = await api.get<BillResponse>('/invoices')
```

**影响**: 编译时无法发现类型错误，运行时可能崩溃

**修复建议**: 为所有 API 接口定义 TypeScript 类型

---

### 2. 前端内存泄漏风险

**位置**: 多个 Vue 组件

**问题**: 
```typescript
// 组件卸载时未清理定时器/事件监听
onMounted(() => {
  setInterval(() => { /* 轮询 */ }, 5000)  // 组件卸载后继续运行
  window.addEventListener('resize', handler)  // 未移除
})
```

**修复建议**:
```typescript
import { onMounted, onUnmounted } from 'vue'

let intervalId: number
let handler: () => void

onMounted(() => {
  intervalId = setInterval(() => { /* ... */ }, 5000)
  window.addEventListener('resize', handler)
})

onUnmounted(() => {
  clearInterval(intervalId)
  window.removeEventListener('resize', handler)
})
```

---

### 3. SQL注入风险

**位置**: `backend/app/api/endpoints/` 多处

**问题**: 部分查询使用字符串拼接

```python
# 潜在风险 ❌
query = db.query(Bill).filter(Bill.seller_name.like(f"%{keyword}%"))
```

虽然 SQLAlchemy 会转义，但如果直接使用原生 SQL：

```python
# 高风险 ❌
db.execute(f"SELECT * FROM bills WHERE seller_name = '{keyword}'")
```

**修复建议**: 始终使用参数化查询

---

### 4. 敏感信息泄露风险

**位置**: `backend/app/api/endpoints/bank_accounts.py:123-125`

```python
# 当前代码
account.auth_token = auth_result.access_token  # 明文存储
account.refresh_token = auth_result.refresh_token
```

**风险**: 数据库泄露时银行凭证直接暴露

**修复建议**:
```python
from cryptography.fernet import Fernet

# 加密存储
cipher = Fernet(settings.ENCRYPTION_KEY)
account.auth_token_encrypted = cipher.encrypt(auth_result.access_token.encode())
```

---

### 5. JWT Token 无过期检查

**位置**: `backend/app/core/permissions.py`

```python
# 当前代码只解码，不验证过期
decode_token(token)  # 缺少过期检查
```

**修复建议**:
```python
from datetime import datetime

def verify_token(token: str) -> dict:
    payload = decode_token(token)
    if payload.get('exp') < datetime.utcnow().timestamp():
        raise TokenExpiredException()
    return payload
```

---

## 🟡 性能问题

### 6. 前端重复渲染

**位置**: `dashboard/index.vue` 等

**问题**: 大量计算属性没有使用 `computed`，每次渲染都重新计算

```typescript
// 当前 ❌
const totalCount = items.value.reduce((sum, item) => sum + item.count, 0)

// 应该 ✅
import { computed } from 'vue'
const totalCount = computed(() => 
  items.value.reduce((sum, item) => sum + item.count, 0)
)
```

---

### 7. 后端查询未优化

**位置**: `audit.py:325`

```python
# 加载所有数据到内存
audited_vouchers = base_query.filter(...).all()  # 可能数万条
```

**应该**:
```python
# 使用数据库聚合
from sqlalchemy import func
result = db.query(func.count(Voucher.id)).filter(...).scalar()
```

---

### 8. 缺少缓存机制

**问题**: 会计科目、规则等静态数据每次都重新加载

```python
# 当前 - 每次请求都执行
@router.get("/accounts")
async def get_accounts():
    return {"items": ACCOUNTS}  # 虽然是常量，但应该缓存响应
```

**应该**:
```python
from fastapi_cache import FastAPICache

@router.get("/accounts")
@cache(expire=3600)  # 缓存1小时
async def get_accounts():
    return {"items": ACCOUNTS}
```

---

## 🟢 代码质量优化

### 9. 前端组件过于庞大

**问题**: 多个组件超过500行

| 文件 | 行数 |
|------|------|
| `bills/components/BillDetailDialog.vue` | ~600行 |
| `customers/components/CustomerDetailDialog.vue` | ~550行 |

**建议**: 拆分为子组件
- `BillDetailDialog` → `BillBasicInfo` + `BillItems` + `BillAttachments`

---

### 10. 后端业务逻辑分散

**问题**: 业务逻辑直接写在 API 端点中

**示例** (bills.py):
```python
# 300+ 行的端点函数，包含验证、业务逻辑、数据库操作
@router.post("")
async def create_invoice(...):
    # 验证
    # 业务逻辑
    # 数据库操作
    # 响应构建
```

**建议**: 使用 Service 层
```python
class BillService:
    def create_bill(self, data: BillCreate) -> Bill:
        self.validate(data)
        bill = self.build_entity(data)
        return self.repository.save(bill)

@router.post("")
async def create_invoice(data: BillCreate, service: BillService = Depends()):
    return service.create_bill(data)
```

---

### 11. 缺少单元测试

**现状**: 
```bash
find backend -name "*test*.py" | wc -l
# 结果: 0
```

**建议测试覆盖率**:
- 核心服务: 80%+
- API端点: 60%+
- 工具函数: 90%+

---

### 12. 前端路由懒加载缺失

**位置**: `router/index.ts`

虽然使用了动态导入，但没有配合 webpack 的魔法注释：

```typescript
// 当前
component: () => import('@/views/bills/index.vue')

// 优化后
component: () => import(/* webpackChunkName: "bills" */ '@/views/bills/index.vue')
```

---

## 🔵 架构优化建议

### 13. 微服务拆分建议

当前单体应用功能过多，建议拆分为:

```
smartledger/
├── api-gateway/          # 网关
├── user-service/         # 用户服务
├── bill-service/         # 票据服务  
├── voucher-service/      # 凭证服务
├── bank-service/         # 银行服务
├── tax-service/          # 税务服务
├── ai-service/           # AI服务
└── notification-service/ # 通知服务
```

---

### 14. 事件驱动架构

当前同步调用过多，建议引入消息队列:

```python
# 当前 - 同步处理
@app.post("/bills")
async def create_bill(data):
    bill = save_bill(data)
    ocr_result = do_ocr(bill.image)  # 阻塞等待
    save_ocr_result(bill.id, ocr_result)

# 优化 - 异步处理
@app.post("/bills")
async def create_bill(data):
    bill = save_bill(data)
    await event_bus.publish('bill.created', bill.id)  # 立即返回
    return bill

# OCR服务订阅事件
@on_event('bill.created')
async def process_ocr(bill_id):
    bill = get_bill(bill_id)
    ocr_result = do_ocr(bill.image)
    save_ocr_result(bill_id, ocr_result)
```

---

## 📊 问题统计

| 类别 | 数量 | 严重程度 |
|------|------|---------|
| 安全问题 | 5 | 🔴 高 |
| 性能问题 | 3 | 🟡 中 |
| 代码质量 | 4 | 🟢 低 |
| 架构问题 | 2 | 🔵 建议 |

---

## 🎯 优化优先级

### 立即 (本周)
1. 🔴 JWT过期检查
2. 🔴 敏感信息加密存储
3. 🔴 修复SQL注入风险点

### 本月
4. 🟡 添加API缓存
5. 🟡 优化数据库查询
6. 🟢 提取Service层

### 下季度
7. 🔵 微服务拆分
8. 🔵 事件驱动改造

---

*报告生成时间: 2026-03-08*